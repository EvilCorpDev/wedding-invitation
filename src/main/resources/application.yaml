server:
  port: 8085

spring:
  application:
    name: wedding-invitation
  datasource:
    url: jdbc:postgresql://localhost:3000/wedding
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
          hibernate:
            default_schema: wedding

logging:
  level:
    org.springframework.security: INFO
